import 'package:flutter/material.dart';

// Warna baru sesuai gambar
const orangeGradient = LinearGradient(
  colors: [Color(0xFFF5851F), Color(0xFFE35D05)],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);

const white = Colors.white;
const black = Colors.black;

const h1 = TextStyle(fontSize: 35, fontWeight: FontWeight.bold, color: white);
const h2 = TextStyle(fontSize: 30, fontWeight: FontWeight.bold, color: white);
const body = TextStyle(fontSize: 14, color: white);
